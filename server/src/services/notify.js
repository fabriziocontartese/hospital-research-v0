const notifyUsers = (userIds, message) => {
  // Stub for MVP: log notifications
  // eslint-disable-next-line no-console
  console.log('[notify]', { userIds, message });
};

module.exports = {
  notifyUsers,
};
